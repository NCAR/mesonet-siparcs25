apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.thingsboardce.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.app.group }}
spec:
  ports:
  - port: {{ .Values.thingsboardce.container.port }}
  selector:
    app: {{ .Values.thingsboardce.name }}