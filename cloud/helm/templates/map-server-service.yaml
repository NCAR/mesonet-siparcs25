apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mapserver.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.app.group }}
spec:
  ports:
  - port: {{ .Values.mapserver.container.port }}
  selector:
    app: {{ .Values.mapserver.name }}