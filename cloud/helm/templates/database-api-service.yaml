apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.databaseapi.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.app.group }}
spec:
  ports:
  - port: {{ .Values.databaseapi.container.port }}
  selector:
    app: {{ .Values.databaseapi.name }}