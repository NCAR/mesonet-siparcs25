apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.metabase.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.app.group }}
spec:
  ports:
  - port: {{ .Values.metabase.container.port }}
  selector:
    app: {{ .Values.metabase.name }}