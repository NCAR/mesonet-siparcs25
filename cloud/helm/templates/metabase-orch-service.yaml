apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.metabaseorch.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.app.group }}
spec:
  ports:
  - port: {{ .Values.metabaseorch.container.port }}
  selector:
    app: {{ .Values.metabaseorch.name }}